<UserControl x:Class="WinDFIR.UI.Views.NetworkView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Network Connections -->
        <GroupBox Grid.Row="0" Header="Network Connections" Margin="5" Padding="5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <DataGrid Name="ConnectionsDataGrid"
                          Grid.Row="1"
                          ItemsSource="{Binding Connections}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          SelectionChanged="ConnectionsDataGrid_SelectionChanged"
                          GridLinesVisibility="All">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Protocol" Binding="{Binding Protocol}" Width="60"/>
                        <DataGridTextColumn Header="Local Endpoint" Binding="{Binding LocalEndpoint}" Width="*"/>
                        <DataGridTextColumn Header="Remote Endpoint" Binding="{Binding RemoteEndpoint}" Width="*"/>
                        <DataGridTextColumn Header="PID" Binding="{Binding ProcessId}" Width="80"/>
                        <DataGridTextColumn Header="State" Binding="{Binding State}" Width="80"/>
                        <DataGridTextColumn Header="Events" Binding="{Binding EventCount}" Width="60"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </GroupBox>

        <!-- Related Events -->
        <GroupBox Grid.Row="1" Header="Related Events" Margin="5" Padding="5">
            <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="200">
                <DataGrid Name="EventsDataGrid"
                          ItemsSource="{Binding RelatedEvents}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          GridLinesVisibility="All"
                          HeadersVisibility="Column"
                          CanUserResizeRows="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Timestamp" Binding="{Binding Timestamp, StringFormat='HH:mm:ss.fff'}" Width="120"/>
                        <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="80"/>
                        <DataGridTextColumn Header="Action" Binding="{Binding Action}" Width="80"/>
                        <DataGridTextColumn Header="Summary" Binding="{Binding Summary}" Width="*"/>
                        <DataGridTextColumn Header="Confidence" Binding="{Binding Confidence}" Width="80"/>
                    </DataGrid.Columns>
                </DataGrid>
            </ScrollViewer>
        </GroupBox>
    </Grid>
</UserControl>
