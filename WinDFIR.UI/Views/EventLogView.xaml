<UserControl x:Class="WinDFIR.UI.Views.EventLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="200"/>
        </Grid.RowDefinitions>

        <!-- Filter Panel -->
        <GroupBox Grid.Row="0" Header="Filters" Margin="5" Padding="5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Start:" VerticalAlignment="Center" Margin="5,0"/>
                <DatePicker Grid.Column="1" Name="StartDatePicker" SelectedDateChanged="StartDatePicker_SelectedDateChanged" Margin="5,0"/>

                <TextBlock Grid.Column="2" Text="End:" VerticalAlignment="Center" Margin="5,0"/>
                <DatePicker Grid.Column="3" Name="EndDatePicker" SelectedDateChanged="EndDatePicker_SelectedDateChanged" Margin="5,0"/>

                <TextBlock Grid.Column="4" Text="Log:" VerticalAlignment="Center" Margin="5,0"/>
                <ComboBox Grid.Column="5" Name="LogFilterComboBox" SelectionChanged="LogFilterComboBox_SelectionChanged" MinWidth="220" Width="Auto" Margin="5,0">
                    <ComboBoxItem Content="All" IsSelected="True"/>
                    <ComboBoxItem Content="Security"/>
                    <ComboBoxItem Content="System"/>
                    <ComboBoxItem Content="Application"/>
                    <ComboBoxItem Content="Microsoft-Windows-PowerShell/Operational"/>
                    <ComboBoxItem Content="Microsoft-Windows-WMI-Activity/Operational"/>
                    <ComboBoxItem Content="Microsoft-Windows-TaskScheduler/Operational"/>
                    <ComboBoxItem Content="Microsoft-Windows-Windows Defender/Operational"/>
                    <ComboBoxItem Content="Microsoft-Windows-Sysmon/Operational"/>
                    <ComboBoxItem Content="Sysmon/Operational"/>
                </ComboBox>

                <TextBlock Grid.Column="6" Text="Search:" VerticalAlignment="Center" Margin="5,0"/>
                <TextBox Grid.Column="7" Name="FilterTextBox" TextChanged="FilterTextBox_TextChanged" Margin="5,0"/>

                <Button Grid.Column="8" Content="Clear" Click="ClearButton_Click" Margin="5,0"/>
            </Grid>
        </GroupBox>

        <!-- Event Log Events -->
        <DataGrid Name="EventLogDataGrid"
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  GridLinesVisibility="All"
                  SelectionMode="Single"
                  SelectedItem="{Binding SelectedEvent}"
                  SelectionChanged="EventLogDataGrid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Timestamp" Binding="{Binding Timestamp, StringFormat='yyyy-MM-dd HH:mm:ss.fff'}" Width="180"/>
                <DataGridTextColumn Header="Log" Binding="{Binding Fields[LogName]}" Width="260"/>
                <DataGridTextColumn Header="EventId" Binding="{Binding Fields[EventId]}" Width="90"/>
                <DataGridTextColumn Header="Level" Binding="{Binding Fields[Level]}" Width="120"/>
                <DataGridTextColumn Header="Source" Binding="{Binding Fields[Source]}" Width="180"/>
                <DataGridTextColumn Header="Summary" Binding="{Binding Summary}" Width="*" MinWidth="300"/>
            </DataGrid.Columns>
        </DataGrid>

        <GridSplitter Grid.Row="2" Height="5" HorizontalAlignment="Stretch" VerticalAlignment="Center" Background="#FFD0D7E5"/>

        <!-- Event Details -->
        <GroupBox Grid.Row="3" Header="Event Details" Margin="5" Padding="5">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBox Name="DetailsTextBlock"
                         IsReadOnly="True"
                         IsUndoEnabled="False"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"
                         Background="Transparent"
                         BorderThickness="0"
                         FontFamily="Consolas"
                         FontSize="11"
                         Margin="5"
                         VerticalScrollBarVisibility="Disabled"/>
            </ScrollViewer>
        </GroupBox>
    </Grid>
</UserControl>
