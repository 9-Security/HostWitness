<UserControl x:Class="WinDFIR.UI.Views.AmcacheView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="5">
        <DataGrid Name="AmcacheDataGrid"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  CanUserAddRows="False"
                  ScrollViewer.HorizontalScrollBarVisibility="Auto">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Type" Binding="{Binding EntryType}" Width="120" MinWidth="100"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="200" MinWidth="160"/>
                <DataGridTextColumn Header="Publisher" Binding="{Binding Publisher}" Width="200" MinWidth="160"/>
                <DataGridTextColumn Header="Version" Binding="{Binding Version}" Width="120" MinWidth="100"/>
                <DataGridTextColumn Header="Product Name" Binding="{Binding ProductName}" Width="220" MinWidth="160"/>
                <DataGridTextColumn Header="Path" Binding="{Binding Path}" Width="320" MinWidth="200"/>
                <DataGridTextColumn Header="SHA1" Binding="{Binding Sha1}" Width="260" MinWidth="200"/>
                <DataGridTextColumn Header="File Id" Binding="{Binding FileId}" Width="220" MinWidth="160"/>
                <DataGridTextColumn Header="File Size" Binding="{Binding FileSize}" Width="100" MinWidth="80"/>
                <DataGridTextColumn Header="Last Write (UTC)" Binding="{Binding LastWriteTimeUtc}" Width="160" MinWidth="140"/>
                <DataGridTextColumn Header="Install Date (Raw)" Binding="{Binding InstallDateRaw}" Width="160" MinWidth="140"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
