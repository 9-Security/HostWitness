<UserControl x:Class="WinDFIR.UI.Views.StaticProcessView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,5">
            <TextBlock Text="{Binding Processes.Count, StringFormat='Processes: {0}'}" VerticalAlignment="Center" FontWeight="Bold"/>
            <TextBlock Text="Loading..." Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                       Foreground="Red" Margin="10,0" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Process View -->
        <DataGrid Name="ProcessDataGrid"
                  Grid.Row="1"
                  ItemsSource="{Binding Processes}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  GridLinesVisibility="Horizontal"
                  SelectionMode="Single"
                  SelectedItem="{Binding SelectedProcess}"
                  PreviewMouseRightButtonDown="ProcessDataGrid_PreviewMouseRightButtonDown"
                  ContextMenuOpening="ProcessDataGrid_ContextMenuOpening">
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Create minidump" Name="CreateMinidumpMenuItem" Click="CreateMinidumpMenuItem_Click"/>
                    <MenuItem Header="Create full dump" Name="CreateFullDumpMenuItem" Click="CreateFullDumpMenuItem_Click" ToolTip="High-risk: full dump includes full process virtual memory (large file)."/>
                </ContextMenu>
            </DataGrid.ContextMenu>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="250"/>
                <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="120"/>
                <DataGridTextColumn Header="Publisher" Binding="{Binding Publisher}" Width="200"/>
                <DataGridTextColumn Header="PID" Binding="{Binding Id}" Width="60"/>
                <DataGridTextColumn Header="Process Name" Binding="{Binding ProcessName}" Width="150"/>
                <DataGridTextColumn Header="Command Line" Binding="{Binding CommandLine}" Width="400"/>
                <DataGridTextColumn Header="Path" Binding="{Binding Path}" Width="*" MinWidth="300"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
