<UserControl x:Class="WinDFIR.UI.Views.AutorunView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="180"/>
        </Grid.RowDefinitions>

        <!-- Filter Panel -->
        <GroupBox Grid.Row="0" Header="Autorun (Registry Run / RunOnce) — data from Offline Hive" Margin="5" Padding="5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="180"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Location:" VerticalAlignment="Center" Margin="5,0"/>
                <ComboBox Grid.Column="1" Name="LocationFilterComboBox" SelectionChanged="LocationFilterComboBox_SelectionChanged" Margin="5,0"/>

                <TextBlock Grid.Column="2" Text="Search:" VerticalAlignment="Center" Margin="10,0,5,0"/>
                <TextBox Grid.Column="3" Name="SearchTextBox" TextChanged="SearchTextBox_TextChanged" Margin="5,0"/>

                <Button Grid.Column="4" Content="Clear" Click="ClearButton_Click" Margin="5,0"/>
            </Grid>
        </GroupBox>

        <!-- Autorun Entries -->
        <DataGrid Name="AutorunDataGrid"
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  GridLinesVisibility="All"
                  SelectionMode="Single"
                  SelectedItem="{Binding SelectedEntry, Mode=TwoWay}"
                  SelectionChanged="AutorunDataGrid_SelectionChanged"
                  Margin="5,0,5,5">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Location" Binding="{Binding Location}" Width="140"/>
                <DataGridTextColumn Header="Entry" Binding="{Binding Entry}" Width="200"/>
                <DataGridTextColumn Header="Command" Binding="{Binding Command}" Width="400"/>
                <DataGridTextColumn Header="Last Write" Binding="{Binding LastWriteTime, StringFormat='yyyy-MM-dd HH:mm:ss'}" Width="160"/>
                <DataGridTextColumn Header="Hive" Binding="{Binding Hive}" Width="*" MinWidth="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <GridSplitter Grid.Row="2" Height="5" HorizontalAlignment="Stretch" VerticalAlignment="Center" Background="#FFD0D7E5"/>

        <!-- Entry Details -->
        <GroupBox Grid.Row="3" Header="Entry Details" Margin="5" Padding="5">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBox Name="DetailsTextBlock"
                         IsReadOnly="True"
                         IsUndoEnabled="False"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"
                         Background="Transparent"
                         BorderThickness="0"
                         FontFamily="Consolas"
                         FontSize="11"
                         Margin="5"
                         VerticalScrollBarVisibility="Disabled"/>
            </ScrollViewer>
        </GroupBox>
    </Grid>
</UserControl>
