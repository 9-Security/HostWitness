<UserControl x:Class="WinDFIR.UI.Views.PrefetchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="200"/>
        </Grid.RowDefinitions>

        <!-- Empty state hint (visible when no items and EmptyReason set) -->
        <TextBlock Grid.Row="0" Margin="12,12,0,0" VerticalAlignment="Top" HorizontalAlignment="Left"
                   Foreground="#666" FontStyle="Italic" TextWrapping="Wrap" MaxWidth="500"
                   Text="{Binding EmptyReason}"
                   Visibility="{Binding EmptyReason, Converter={StaticResource StringToVisibilityConverter}}"/>

        <!-- Prefetch list (top) -->
        <DataGrid Name="PrefetchDataGrid"
                  Grid.Row="0"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  GridLinesVisibility="All"
                  SelectionMode="Single"
                  SelectedItem="{Binding SelectedItem}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Filename" Binding="{Binding Filename}" Width="220"/>
                <DataGridTextColumn Header="Created Time" Binding="{Binding CreatedTime, StringFormat='yyyy-MM-dd HH:mm:ss'}" Width="160"/>
                <DataGridTextColumn Header="Modified Time" Binding="{Binding ModifiedTime, StringFormat='yyyy-MM-dd HH:mm:ss'}" Width="160"/>
                <DataGridTextColumn Header="File Size" Binding="{Binding FileSize}" Width="90"/>
                <DataGridTextColumn Header="Process EXE" Binding="{Binding ProcessExe}" Width="140"/>
                <DataGridTextColumn Header="Process Path" Binding="{Binding ProcessPath}" Width="400"/>
                <DataGridTextColumn Header="Run Count" Binding="{Binding RunCount}" Width="90"/>
                <DataGridTextColumn Header="Last Run Time" Binding="{Binding LastRunTime, StringFormat='yyyy-MM-dd HH:mm:ss'}" Width="*" MinWidth="160"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Splitter -->
        <GridSplitter Grid.Row="1" Height="5" HorizontalAlignment="Stretch" Background="#DDDDDD" ShowsPreview="True"/>

        <!-- Referenced files list (bottom) -->
        <DataGrid Name="ReferencedFilesDataGrid"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  GridLinesVisibility="All">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Filename" Binding="{Binding FileName}" Width="250"/>
                <DataGridTextColumn Header="Full Path" Binding="{Binding FullPath}" Width="*" MinWidth="200"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
