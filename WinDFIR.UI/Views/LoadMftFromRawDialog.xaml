<Window x:Class="WinDFIR.UI.Views.LoadMftFromRawDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Load MFT from Volumes"
        Width="460" Height="320"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0"
                   Text="Select one or more NTFS volumes to load their MFT tables. The reader tries raw volume access first, then backup privilege, then VSS if needed. Administrator rights are required."
                   TextWrapping="Wrap"
                   Margin="0,0,0,10"/>
        <TextBlock Grid.Row="1"
                   Text="Use Ctrl or Shift to select multiple volumes."
                   Margin="0,0,0,6"/>
        <ListBox Grid.Row="2"
                 Name="DriveLetterList"
                 SelectionMode="Extended"
                 Height="170"/>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="OK" IsDefault="True" Click="Ok_Click" Width="80" Margin="0,0,8,0"/>
            <Button Content="Cancel" IsCancel="True" Width="80"/>
        </StackPanel>
    </Grid>
</Window>