# HostWitness 問題記錄 (2026-03-13)

依您要求：**先記錄問題，全部都回報完再修改程式碼**。以下為當時已知問題與建議項目的完整清單。

**後續修正（2026-03-20）**：下列 **#1、#2、#3** 已於程式碼與文件中處理（見各節「狀態」）。其餘項目維持原記錄供對照。

---

## 一、程式碼與資源

### 1. AddProtocolToggle 可能回傳 null（潛在 NRE）

| 項目 | 說明 |
|------|------|
| **狀態** | **已修正（2026-03-20）**：`AddProtocolToggle` 回傳 `ToggleButton?`，資源缺失時 `return null`；LiveTcp 分離工具列 handler 以 `if (tcpV4 != null)` 等同步主視窗切換，不再使用 `tcpV4!`。 |
| **位置** | `WinDFIR.UI\MainWindow.xaml.cs`（`CreateDetachedToolBar` 內之 `AddProtocolToggle` 與 LiveTcp 區塊） |
| **原狀況** | 曾回傳 `null!` 且回傳型別為非 nullable `ToggleButton`；呼叫端使用 `tcpV4!` 等。 |
| **原風險** | 資源缺失時變數為 null；若未處理可能造成 NRE。 |

---

### 2. TODO：P/Invoke 文件同步

| 項目 | 說明 |
|------|------|
| **狀態** | **已收口（2026-03-20）**：已移除 inline TODO；改為註解內明確維護條款，並於 `docs\TECH_DEBT.md` §1／§5、`docs\RegistrySearch說明.md` 寫入「P/Invoke 或 handle 用法變更須同變更更新文件」。新增 `RegistrySearchProviderTests.RunQueriesAsync_HkcuEnvironment_ProducesRegistryCategoryEvents` 作為 Live + LastWriteTime 路徑回歸測試。 |
| **位置** | `WinDFIR.Providers\RegistrySearchProvider.cs`（P/Invoke 區域註解） |
| **原內容** | 曾為 `// TODO: 若 P/Invoke 簽章或行為說明需更正…` |

---

### 3. 測試使用已標記 Obsolete 的 API

| 項目 | 說明 |
|------|------|
| **狀態** | **已調整（2026-03-20）**：新增主路徑測試 `GenerateNetworkFlowKey_FromHostPortParts_MatchesLegacyNetworkKeyShape`；舊 API 測試改名為 `GenerateNetworkKey_LegacyObsoleteApi_StillPopulatesFields` 並以 `#pragma warning disable CS0618` 標示為相容性測試（不再在測試方法上使用 `[Obsolete]`）。 |
| **位置** | `WinDFIR.Tests\KeyGeneratorTests.cs` |
| **後續** | 正式移除 `GenerateNetworkKey`／`NetworkKey` 時可刪除相容性測試一項。 |

---

### 4. 刻意 null 壓制（無誤但可讀性）

| 項目 | 說明 |
|------|------|
| **位置** | 多處 |
| **範例** | `SnapshotImporter.cs` 約 94 行 `activityEvent = null!`（out 參數）、`RecentFilesViewModel.cs` 約 22 行 `SourceEvent = null!`、`PrefetchViewModel.cs` 約 22 行 `Record = null!`。 |
| **說明** | 屬刻意壓制，語意正確；若希望減少 `null!`，可改為可為 null 型別並在首次使用前賦值。 |

---

## 二、建置與資源

### 5. LOGOS 路徑與檔案

| 項目 | 說明 |
|------|------|
| **位置** | `WinDFIR.UI\WinDFIR.UI.csproj`：`ApplicationIcon` 與 `Resource` 指向 `..\LOGOS\icon.ico`、`..\LOGOS\程式圖案.png`。 |
| **現狀** | 專案根目錄下 **LOGOS 資料夾存在**，內有 `icon.ico` 與中文檔名圖檔（主控台可能顯示為 ????）。 |
| **建議** | 確認 `程式圖案.png` 確實在 LOGOS 內且檔名編碼正確；若建置報錯，檢查路徑與 Link。 |

---

## 三、技術債與架構（已文件化）

以下為文件中已記錄、可選或中長期處理項目，不影響「先記錄再改」的順序。

### 6. Registry Live 模式

- **文件**：`TECH_DEBT.md` §1、`LIMITATIONS.md` §2。
- **內容**：Live Registry + P/Invoke 為過渡方案；鑑識優先使用 Offline Hive。UI 已標示「Live (non-forensic)」。

### 7. Session / Index 持久化

- **文件**：`TECH_DEBT.md` §3、`待修復問題記錄.md`。
- **內容**：Session 還原已實作；完整 SQLite/二進位 Index 持久化為可選、中長期。

### 8. Docking / 獨立視窗

- **文件**：`TECH_DEBT.md` §4。
- **內容**：Detach/Restore 已實作；完整 Docking（拖曳、儲存版面）為可選，需較大 UI 重構。

### 9. Process.Start / ShellExecute

- **狀態**：**已收斂（2026-03-20）**：`WinDFIR.UI.Services.ShellLaunchHelper` 統一處理；Settings 說明連結與 Process 檢視「開啟目錄」已改呼叫該 helper（允許規則見 `docs\TECH_DEBT.md`「中長期排程」優先 2）。
- **原位置**：`SettingsWindow.xaml.cs`、`ProcessView.xaml.cs`（曾直接 `Process.Start`）。

### 10. SqliteIndexPersistence / 連線字串

- **文件**：TECH_DEBT、LIMITATIONS。
- **內容**：`dbPath` 由呼叫端提供；已使用 parameterized 查詢，無直接 SQL 注入；路徑信任需由呼叫端保證。

---

## 四、已知風險（已緩解／文件化）

- **檔案鎖定** → VSS 回退。
- **Rootkit/API Hooking** → 文件說明，Raw 為未來方案。
- **OOM** → MaxEvents/Eviction。
- **ETW 高頻** → 節流與丟棄計數可見。
- 詳見 `docs\LIMITATIONS.md` §14。

---

## 五、摘要表（供修改程式碼時對照）

| # | 類別         | 位置/說明 | 優先級 | 是否需改程式碼 |
|---|--------------|-----------|--------|----------------|
| 1 | 潛在 NRE     | MainWindow `AddProtocolToggle` → **已修正** | — | 已完成 |
| 2 | P/Invoke 文件 | RegistrySearchProvider + TECH_DEBT 等 → **已收口** | — | 已完成 |
| 3 | Obsolete 測試 | KeyGeneratorTests → **主路徑 + 相容性測試已分離** | — | 已完成 |
| 4 | null! 壓制   | ViewModel / SnapshotImporter 等 | 低 | 可選（可讀性） |
| 5 | LOGOS        | 確認 程式圖案.png 存在與路徑 | 低 | 僅確認，必要時改 csproj |
| 9 | ShellExecute | `ShellLaunchHelper` → **已收斂** | — | 已完成 |
| 6–8, 10 | 技術債/安全 | 已文件化 | 可選 | 依排程 |

---

**下一步**：#1–#3 已處理。其餘可自 #4 起依排程處理（可選）。
