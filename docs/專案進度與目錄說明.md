# 專案進度與目錄說明 (2026-03-19)

## 目前專案進度摘要

- **狀態**：Build Success（2026-03-19 驗證）；已發布 `Release\HostWitness.exe`，並已整理根目錄暫存輸出，只保留正式 `Release\` 與必要 `Release_Verification\` 報告。
- **近期完成**：MFT 改為 **per-source tabs**（`Load from Volumes...` 可一次選多個磁區，`C:` / `D:` 各自獨立表格、分頁、篩選、匯出與狀態），並移除會重跑來源的 Refresh；MFT 從磁碟載入維持 **raw volume → Backup privilege → VSS** ordered fallback 與非阻塞進度顯示。Snapshot 匯出會改寫 evidence 成 bundle-local `raw/` 路徑，Open Snapshot / Export to SQLite / Export Snapshot 改為背景化，Session 還原會提示目前 live index 容量上限；另已做 dead code cleanup（移除未使用的 MFT private wrapper 與 `MainWindow` 中未讀取的 MFT view accessor）。其餘：Code review 與潛在 bug 修正、Prefetch v31、Help → About 著作權；版面儲存/還原、SQLite、Raw Disk、Session 還原等。
- **核心能力**：VSS 快照、Activity Index（可配置 MaxEvents、SQLite 匯出/開啟）、Live Process / Live TCP（Sent/Recv 由 ETW 接上）/ Timeline / Autorun、Offline Hive（含 Raw Disk 設定）、Registry Search（可編輯與僅離線）、Amcache/Prefetch（v10–31）/Event Log/Netstat/Browsing History/**MFT（raw volume / Backup privilege / VSS fallback、每來源獨立分頁、無 Refresh 重跑）**、Drill-down、Create Dump；版面儲存/還原、scripts 排程與自動回傳。
- **待辦（可延後）**：詳見 `待修復問題記錄.md`。

## 目錄結構（精簡後）

```
HostWitness/
├── .gitignore
├── create-signing-cert.ps1                             # 自簽憑證
├── publish.cmd                                         # 編譯與發布腳本
├── scripts/                                            # 遠端採集排程與回傳腳本（ScheduledCollect.ps1、RunCollectAndCopy.ps1）
├── README.md                                           # 專案說明與編譯
├── docs/                                               # 說明文件
│   ├── README.md                                       # 本目錄說明
│   ├── 待修復問題記錄.md                                # 進度與待修正
│   ├── 專案進度與目錄說明.md                            # 本檔
│   ├── ARCHITECTURE.md
│   ├── LIMITATIONS.md
│   ├── FORENSIC_ASSUMPTIONS.md
│   ├── EventLogView說明.md、PrefetchView說明.md、AmcacheView說明.md
│   ├── RegistrySearch說明.md
│   └── TimelineView優化說明.md
├── LOGOS/                                              # 程式圖示
├── Release/                                            # 正式發布輸出
├── Release_Verification/                               # 驗證報告輸出（選用）
├── WinDFIR.Core/                                       # 核心
├── WinDFIR.Providers/                                 # 提供者
├── WinDFIR.Tests/                                      # 單元測試
└── WinDFIR.UI/                                         # WPF UI
```

## 已移除的冗餘項目 (2026-02-02)

- **根目錄暫存檔**：`build_err.txt`、`build_error.txt`、`providers_error.txt`、`providers_error_2.txt`、`providers_error_3.txt`、`providers_error_utf8.txt`（建置/除錯用錯誤日誌，已加入 .gitignore 避免再次納版）。
- **old_docs/**：歷史修復與舊版說明共 24 個檔案，內容已整併至 `待修復問題記錄.md` 與各現行說明；目錄已刪除以精簡專案，若有需要可自版控歷史還原。

**2026-03-05 目錄整理**：清除建置產物 `bin/`、`obj/`（各專案），以及選用發布目錄 `Release_SingleFile/`；`.gitignore` 已加入 `Release_SingleFile/`。`Release/` 為 `publish.cmd` 輸出，若需重產請執行 `.\publish.cmd`。

**2026-03-19 目錄整理**：移除 `Release_publish_probe/`、根目錄 `publish_diagnostic.log` / `providers_publish_diagnostic.log` / `ps_build_diag.log`，並將 `Release_Verification/` 僅保留正式 release 與最新 workspace 驗證報告。各專案 `bin/obj` 仍屬可刪生成物；若檔案被執行中程序鎖定，關閉相關程序後可再清。

**文件更新：** 2026-03-19（同步移除未使用的 MFT private wrapper 與 `MainWindow` 中未讀取的 MFT view accessor）；2026-03-19（專案狀態同步、建置驗證）；2026-03-18（MFT 改為 per-source tabs、`Load from Volumes...` 多選與移除 Refresh）；2026-03-17（MFT 改為 raw volume → Backup privilege → VSS ordered fallback，並補上進度顯示）；2026-03-16（Snapshot evidence remap、背景化匯入/匯出、Session restore 容量提示、dead code cleanup）；2026-03-13（專案狀態與目錄結構更新）


