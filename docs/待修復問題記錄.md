# 專案開發進度與修復記錄 (2026-03-20 更新)

## 🎯 目前進度摘要
**2026-03-20 短期技術債收斂**：LiveTcp 分離工具列 `AddProtocolToggle` 已改為 nullable 流程並移除 `null!` 風險；RegistrySearchProvider 的 P/Invoke／文件同步規則已收斂至程式註解、`TECH_DEBT.md`、`LIMITATIONS.md` §2 與 `RegistrySearch說明.md`；`KeyGenerator` 舊 API 測試已改為相容性測試，並新增 `GenerateNetworkFlowKey` 主路徑測試與 `RegistrySearchProvider` Live + `RegQueryInfoKey` 回歸測試。當次 `dotnet build WinDFIR.sln -c Release` 通過，當次 `dotnet test WinDFIR.sln -c Release` 為 82 / 82 通過；**最新 stable gate 測試結果見文末編譯狀態（95 / 95）**。
**2026-03-19 專案狀態同步**：專案已做調整優化，文件與建置狀態已同步；Release 建置成功、文件更新日期與目錄說明一致。詳見根目錄 README「文件更新」與本目錄各說明。
**2026-03-05 Code review 與潛在 bug 修正**：依審查優先級完成：Sqlite 版本統一（8.0.11）、RawDiskReader 讀取上限（100 MB）、匯出/儲存筆數上限（50 萬）、路徑驗證（Settings 說明連結、SQLite BuildConnectionString）、MainWindow async 例外處理、Amcache 路徑 `..` 防護、MftParser attrLength 上限、Prefetch uncompressedSize 上限、LoadMftFromRawDialog Size 上限、Registry Search 取消權杖。潛在 bug：Session 儲存失敗時 MessageBox 提示、空 catch 改為 Debug.WriteLine（RegistrySearchProvider、SystemInfoViewModel、StaticProcessViewModel、AmcacheViewModel）、各 ViewModel Dispatcher.InvokeAsync 關閉時防護（InvalidOperationException）。功能：Prefetch 支援 version 31（Windows 11）、空清單原因提示；MFT Load from Raw Disk 可選擇磁碟機（C:、D:）；Help → About 著作權訊息。
**2026-03-05 建議修復／測試實作**：SnapshotExporterTests 新增 ExportAsync_Manifest_HasRequiredHostFields（manifest 必要欄位與 machineSid）；BrowserHistoryProviderTests 新增 StartAsync_ThenStopAsync、StopAsync_BeforeProcessingCompletes（驗證 CancellationToken）；ParserRobustnessTests 新增 ActivityEventNormalizer_NormalizeAction_NullOrWhiteSpace_ReturnsQuery。BrowserHistory 深度 8 與 CancellationToken 已存在；單元測試 38 則全數通過。
**2026-03-05 當時列為尚未完成／可選功能的項目**：完整 Docking UI、Index 完整持久化、遠端採集排程/回傳自動化、Live TCP Sent/Recv、Raw Disk 導入、Registry 僅 Offline 選項強化。上述項目於後續輪次多已完成；其中 **Docking** 目前完成的是 `Detach/Restore` 基線，**Index** 目前完成的是 session save/restore + SQLite 匯出/開啟，完整形態仍屬中長期題目。現況請以 `docs\TECH_DEBT.md`、`docs\還有什麼要做的事項.md` 與 `docs\穩定版邊界定義.md` 為準。
**2026-03-05 Session 還原與技術債**：關閉時自動儲存時間軸至 `%AppData%\HostWitness\last_session`，啟動時可還原；File → Save session now 手動儲存（SessionPersistence）。技術債四項：Registry Live 標示為「非鑑識」、ViewRegistryService 抽出視圖/Detach 註冊表、P/Invoke 註解與文件連結；TECH_DEBT.md 全量更新。
**2026-03-05 文件與發布**：更新 README、使用說明、專案進度、待修復問題記錄、TECH_DEBT；README 新增「後續待辦與可選項目」清單；執行 **`publish.cmd`** 產生最新 Release 執行檔。
**2026-03-05 功能與技術債（前輪）**：Registry Search 預設查詢可編輯（Value/Data pattern、Recursive）、僅離線登錄檔選項與選單/對話框說明、Settings 匯出預設路徑與 Registry 說明連結、Timeline 匯出檔名/內容日期範圍與 N 筆/空結果/錯誤提示、工具列與 Detach 按鈕由 ViewModel 驅動。
**2026-03-05 待修補問題處理**：SnapshotExporter 改為單次 `GetEventsByTimeRange` 取得事件清單後共用於 timeline / entities / raw 匯出，避免重複遍歷與記憶體壓力；StubProvider 改為僅在 DEBUG 組態註冊，Release 版不再產生 mock 事件。**2026-03-05 建議項目**：修復 6 個失敗單元測試（InMemoryActivityIndex 改為比對關鍵屬性、ProcessKeyCache 第二呼叫時間改為同 TTL 內）；BrowserHistory 掃描改為深度上限 8、支援 CancellationToken；新增 SnapshotExporterTests 驗證 manifest machineSid 格式；review 標註 BrowserHistory／_events 已處理或 N/A。**功能與效能**：新增 `docs\RawDisk使用說明.md`、README/LIMITATIONS 指向該說明；OfflineHiveRegistryProvider.EnumerateKeys 改為迭代器（TraverseYield），大型 subtree 不再先建完整 List；WinDFIR.UI.csproj 加入 PublishSingleFile/IncludeAllContentForSelfExtract 選項與 README 單檔發布說明。**功能全量**：MFT 解析器（Core.Mft）與 MFT 靜態分頁 UI；遠端採集 WinDFIR.Agent（主控台、無 UI、採集後匯出 Snapshot），見 `docs\遠端採集Agent說明.md`。**效能**：LiveProcessProvider 缺 map 時改為單次 GetProcessDetailsSingle(pid) 取代 3 次 WMI。**測試**：ProcessKeyConsistencyTests、ParserRobustnessTests（MftParser 空串流、ActivityEventNormalizer 空/空白 Action）；review Missing Tests 已標註已補。  
近期完成 **Detach/Restore 與分離視窗圖示統一**（主視窗 Detach/Restore 僅圖示、ToolTip 切換；分離後浮動視窗工具列改為圖示按鈕：Play/Pause、Clear、Refresh、Restore、Resolve、States、TCP/UDP 篩選，與主視窗一致）；**啟動預設 System Info**（MainWindow_Loaded 強制靜態分頁選中 System Info、動態分頁不預選）；**Offline Hive 離線一致性流程強化**（OfflineHiveSource/ConsistencyScope/SnapshotTimeUtc、Mixed 警告）；**DestList 對應精準化**；**時區處理**（Settings TimeZoneDisplay Local/UTC）；**Prefetch 版本驗證**（17/23/26/30）；**Action 正規化大小寫**；**風險緩解表**（LIMITATIONS §14）、**技術債文件化**（ARCHITECTURE §7）。**重新發布**：2026-02-02 已執行 **`publish.cmd`** 產生簽章 `Release\HostWitness.exe`。維持既有 VSS、Activity Index、Live Process / Live TCP / Timeline / Autorun / Offline Hive 分析能力。

---

## ✅ 已完成功能與異動項目

### 1. Live Process Procmon-like 強化 - 🆕 *新增*
*   **功能**: 導入 Procmon 風格的過濾與操作流程。
*   **重點**:
    *   **Filter Bar** + 規則列表（Include/Exclude、欄位與運算子）。
    *   **Columns** 欄位顯示管理（可勾選顯示），設定會保存到 `%AppData%\HostWitness\process_view_columns.json`。
    *   **Capture On/Paused**：只暫停畫面更新，背景持續收集。
    *   **右鍵功能**：Apply as Filter（含 ProcessName/PID/ImagePath/Company/Hash）、Copy Value、Image Path 可 Open Directory、**Create Dump > Create minidump / Create full dump**。**Show process tree** 時 TreeView 亦有相同右鍵選單。
    *   **結果高亮**：依 Last Result 顯示顏色提示（Access Denied/Not Found/Buffer Overflow）。
    *   **欄位補強**：Parent PID / Integrity / Image Path / Company / Hash / Last Operation / Last Result。
*   **涉及檔案**:
    *   `WinDFIR.UI\Views\ProcessView.xaml`
    *   `WinDFIR.UI\Views\ProcessView.xaml.cs`
    *   `WinDFIR.UI\ViewModels\ProcessViewModel.cs`
    *   `WinDFIR.UI\Converters\ResultSeverityToBrushConverter.cs`
    *   `WinDFIR.UI\Converters\BindingProxy.cs`

### 2. Live TCP View (TCPView-like) - 🆕 *新增*
*   **功能**: 動態分析區新增 Live TCP View，提供 TCPView 類似的即時連線列表與搜尋。
*   **重點**:
    *   動態分頁新增 **Live TCP View**。
    *   靜態分頁 **Network View** 改名為 **Netstat**（功能不變）。
    *   即時列表對齊 TCPView 欄位：Process Name / Process ID / Protocol / State / Local Address / Local Port / Remote Address / Remote Port / Create Time / Module Name / Sent/Recv Packets / Sent/Recv Bytes。
    *   工具列功能：Resolve Address、States Filter、TCP/UDP v4/v6 篩選，並提供圖示與按壓動畫。
    *   States Filter 視窗改為 TCPView 風格（All/None/Invert/OK/Cancel），選項與圖示對齊。
    *   所有 Refresh 改為綠色圓形圖示並移至功能區。
    *   Resolve Address 支援 IP/FQDN 互轉，圖示改為 TCPView 風格。
    *   Live TCP View 移除 Image Path 欄位，欄位對齊 TCPView。
    *   Live Process 篩選條件顯示所有規則，支援 AND/OR 組合。
    *   Live Process 資訊區支援水平滾動條。
    *   Live Process 新增 Process Tree（祖父/父/子）顯示與隱藏 OS 程序。
    *   Live Process Show Process Tree 顯示修正（TreeView 即時刷新）。
    *   Show Process Tree 改為 Visibility 直綁（含反向 Converter），避免樣式覆蓋導致永遠隱藏。
    *   LiveProcessProvider 改用 Win32_Process 全表補 ParentProcessId/CommandLine/ExecutablePath，樹狀關係可正確建立。
    *   BootId 計算單位修正（秒/毫秒一致，避免同次開機不一致）。
    *   EventLog/ETW ProcessKey CreateTime 來源修正（避免同 PID 事件分裂）。
    *   LiveProcessProvider StartTime 失敗時改用快取/單次回退，避免誤判啟停。
    *   BrowserHistory URL NULL 防護，避免整檔跳過。
    *   Snapshot manifest machineSid 改為穩定 SID 推導（Admin SID 前綴/ MachineGuid 回退）。
    *   VSS ResolvePath 非法路徑防護。
    *   UI 事件佇列計數修正，避免超過上限後永久停止更新。
    *   EventLog/ETW PID 建立時間快取加入 Stop 清除與 TTL（避免 PID 重用污染）。
    *   EventLog/ETW PID 快取依可靠度分級（Provisional/Authoritative）並使用不同 TTL。
    *   PID 快取加入 LRU 上限與長駐程序例外（Long‑lived 清單）。
    *   新增 PID 快取狀態顯示與 settings.json 可調參數。
    *   Advanced → Settings 設定視窗（可視覺化調整 PID 快取參數）。
    *   Advanced → Settings 新增全介面字型大小（小/中/大/自訂）。
    *   Settings 視窗 crash 修正（Notes binding OneWay）。
    *   Netstat Related Events 顯示穩定（Refresh 不再清空選取）。
    *   Recent Files / Event Log 詳細資訊可拖曳調整高度。
    *   ETW 節流狀態列顯示（last drops）。
    *   LiveProcess 建立時間未知改用旗標更新，避免後續「假重啟」。
    *   程式視窗 Icon 改為 `LOGOS/程式圖案.png`。
    *   動態分頁支援 Detach/Restore，可拆出獨立視窗同時觀察。
    *   Sent/Recv 封包與位元組當時仍為佔位值（尚未接 ETW 計數）；**此為歷史狀態**，現況見後文「Live TCP Sent/Recv：已接入 ETW Kernel-Network 計數」。
    *   Offline Hive 解析支援 VSS 快照路徑（需管理員權限；失敗時回退 Live 路徑）。
    *   Offline Hive 補齊高價值 key（AppCompatCache、UserAssist、MountPoints2、ComDlg32、Uninstall、AppCompatLayers 等）。
    *   InMemoryActivityIndex Evicted 事件增加系統提示（每 5 秒彙整一次）。
    *   InMemoryActivityIndex 改為有界佇列，保留最新事件並記錄 Evicted。
    *   ETW 節流新增丟棄事件計數提示。
    *   CustomDestinations 深度解析：優先結構化儲存 (CFB) + DestList 對應，失敗時回退 Raw 解析。
    *   Amcache View：Amcache.hve 解析與靜態檢視（含 VSS 快照回退）。
*   **涉及檔案**:
    *   `WinDFIR.UI\Views\LiveTcpView.xaml` (New)
    *   `WinDFIR.UI\Views\LiveTcpView.xaml.cs` (New)
    *   `WinDFIR.UI\ViewModels\LiveTcpViewModel.cs` (New)
    *   `WinDFIR.UI\MainWindow.xaml`
    *   `WinDFIR.UI\MainWindow.xaml.cs`

### 3. Detach/Restore 與分離視窗圖示、啟動預設 System Info - 🆕 *新增*
*   **功能**: 主視窗 Detach/Restore 按鈕僅顯示圖示（ToolTip 切換「Detach」/「Restore」）；分離後的浮動視窗工具列與主視窗一致，改為圖示按鈕（Play/Pause、Clear、Refresh、Restore、Resolve、States、TCP/UDP 篩選）；程式啟動時預設選中 **System Info** 分頁。
*   **重點**:
    *   Window.Resources 新增 DetachIcon、RestoreIcon、PlayIcon（發佈版 FindResource 可解析）；UpdateDetachButtonState 使用 TryFindResource 設定圖示。
    *   CreateDetachedToolBar 改為 AddIconButton/AddIconToggle/AddProtocolToggle，資源自 DynamicToolBar 取得。
    *   MainWindow_Loaded 強制 StaticTabControl.SelectedIndex=0、DynamicTabControl.SelectedIndex=-1，並呼叫 UpdateSharedContent()。
*   **涉及檔案**:
    *   `WinDFIR.UI\MainWindow.xaml`（Window.Resources、PlayIcon、Loaded）
    *   `WinDFIR.UI\MainWindow.xaml.cs`（CreateDetachedToolBar、MainWindow_Loaded）

### 4. 啟動問題診斷 - 🆕 *新增*
*   **問題**: 近期 UI 綁定循環導致程式無法啟動（無視窗、無錯誤）。
*   **修正**: 加入啟動例外捕捉與 `startup.log`（`%AppData%\HostWitness\logs\startup.log`）。
*   **涉及檔案**:
    *   `WinDFIR.UI\App.xaml`
    *   `WinDFIR.UI\App.xaml.cs`

### 5. 介面架構整合 (UI Consolidation)
*   **異動內容**: 將原本割裂的 Dynamic/Static 雙分頁架構統一為單一 `MainTabControl`。
*   **優點**: 操作更直覺，消除佈局空間浪費。
*   **涉及檔案**:
    *   `WinDFIR.UI\MainWindow.xaml`: 修改 Grid 佈局與 TabControl 結構。
    *   `WinDFIR.UI\MainWindow.xaml.cs`: 重寫索引邏輯與檢視更新機制。

### 5. 靜態程序清單 (Process List View) - 🆕 *新增*
*   **功能**: 提供類 Task Manager 的詳細當前程序列表。
*   **特色**: 支援 Publisher (數位簽章發行者) 識別與 WMI 指令列提取。
*   **涉及檔案**:
    *   `WinDFIR.UI\Views\StaticProcessView.xaml` (New)
    *   `WinDFIR.UI\Views\StaticProcessView.xaml.cs` (New)
    *   `WinDFIR.UI\ViewModels\StaticProcessViewModel.cs` (New)

### 7. 時間軸優化 (Timeline View Optimization)
*   **優化項目**:
    *   實現 **精準排序 (由新到舊)**。
    *   **選擇狀態保留**: 重新整理資料時，原本選中的行不會位移或消失。
    *   **詳細資訊區域**: 新增可拖曳高度的 Splitter。
*   **涉及檔案**:
    *   `WinDFIR.UI\Views\TimelineView.xaml`
    *   `WinDFIR.UI\ViewModels\TimelineViewModel.cs`

### 8. 註冊表搜尋提供者 (Registry Search Provider) - 🆕 *新增*
*   **功能**: 支援自定義 KeyPath 與 Pattern 查詢，自動提取高價值機碼。
*   **鑑識增強**: 透過 P/Invoke **`RegQueryInfoKey`** 獲取 **LastWriteTime**，使 Registry 事件能準確排序在時間軸上。
*   **涉及檔案**:
    *   `WinDFIR.Providers\RegistrySearchProvider.cs` (New)
    *   `WinDFIR.UI\MainWindow.xaml.cs` (註冊 Provider)

### 8. Recent Files 統一視圖 (Recent Files View) - 🆕 *新增*
*   **功能**: 整合 RecentDocs MRU、Recent LNK、JumpList (Automatic/Custom) 於單一視圖。
*   **特色**: 支援 MRU 排序、Link times、DestList 對應、Path/Name fallback。
*   **涉及檔案**:
    *   `WinDFIR.UI\Views\RecentFilesView.xaml` (New)
    *   `WinDFIR.UI\ViewModels\RecentFilesViewModel.cs` (New)
    *   `WinDFIR.Providers\RegistrySearchProvider.cs` (RecentDocs 解析)
    *   `WinDFIR.Providers\RecentLnkProvider.cs` (LNK 深度解析)
    *   `WinDFIR.Providers\JumpListProvider.cs` (DestList/Custom 解析)

### 10. Offline Hive 解析導入 - 🆕 *已強化*
*   **功能**: 離線 Hive 解析 (Registry 套件)，讀取 SYSTEM/SOFTWARE/NTUSER/USRCLASS；已擴充 key（TimeZone/ComputerName/Select/Windows、StartupApprovedRun/ProfileList/InternetSettings、OpenSaveMRU/Streams、Bags）。
*   **VSS**: 已支援 VSS 快照路徑（`VssSnapshotService` 權限與服務預檢、WMI 錯誤碼對照、部分成功回退、HasSnapshotForPath、CreationTimeUtc）；失敗時回退 Live 路徑。Raw Disk 仍未導入。
*   **離線一致性**: 事件欄位 OfflineHiveSource (VSS/Live)、ConsistencyScope (SingleSnapshot/SingleLive/Mixed)、SnapshotTimeUtc；Mixed 時發出警告。
*   **涉及檔案**:
    *   `WinDFIR.Providers\OfflineHiveRegistryProvider.cs`
    *   `WinDFIR.Core\Snapshot\VssSnapshotService.cs`

### 10. Event Log View - 🆕 *新增*
*   **功能**: 專門顯示 Event Log 事件，支援 LogName 篩選與全文搜尋。
*   **涉及檔案**:
    *   `WinDFIR.UI\Views\EventLogView.xaml` (New)
    *   `WinDFIR.UI\ViewModels\EventLogViewModel.cs` (New)

### 12. Prefetch View - 🆕 *新增*
*   **功能**: 類 WinPrefetchView 的 Prefetch 清單與參考檔案清單（含選取保持）。
*   **涉及檔案**:
    *   `WinDFIR.UI\Views\PrefetchView.xaml` (New)
    *   `WinDFIR.UI\ViewModels\PrefetchViewModel.cs` (New)
    *   `WinDFIR.Providers\Parsers\PrefetchParser.cs` (New)

### 13. 待修補問題處理 (2026-03-05) - 🆕 *優化*
*   **SnapshotExporter 單次查詢**：ExportAsync 改為一次 `GetEventsByTimeRange(DateTime.MinValue, DateTime.MaxValue).ToList()`，將結果傳入 ExportTimelineAsync、ExportEntitiesAsync、ExportRawArtifactsAsync，避免連續三次全量查詢與重複遍歷（對應歷史 code review 的 Optimizations）。
*   **StubProvider 僅 Debug**：MainWindow 註冊 providers 時以 `#if DEBUG` 包住 StubProvider，Release 版不再註冊，避免多餘 mock 事件與依賴（對應歷史 code review 的 Slimming）。
*   **單元測試修復與新增**：InMemoryActivityIndexTests 改為比對關鍵屬性（因 index 儲存 normalized 事件）；ProcessKeyCacheTests 第二呼叫改為同 TTL 內時間，避免快取過期；新增 SnapshotExporterTests 驗證 ExportAsync 產出之 manifest 含有效 machineSid 格式。
*   **BrowserHistory 掃描優化**：GetBrowserHistoryPaths 接受 CancellationToken；AddHistoryFilesByScan 改為深度上限 8、支援取消；已知路徑優先不變（對應歷史 code review 的 Optimizations）。
*   **歷史 review 條目同步**：BrowserHistory、_events 條目標為已處理／不適用。
*   **涉及檔案**:
    *   `WinDFIR.Core\Snapshot\SnapshotExporter.cs`
    *   `WinDFIR.UI\MainWindow.xaml.cs`
    *   `WinDFIR.Providers\BrowserHistoryProvider.cs`
    *   `WinDFIR.Tests\InMemoryActivityIndexTests.cs`
    *   `WinDFIR.Tests\ProcessKeyCacheTests.cs`
    *   `WinDFIR.Tests\SnapshotExporterTests.cs` (New)

---

## 📝 已更新之說明文件
1.  `docs\TimelineView優化說明.md`: 詳細說明 Timeline 的視覺與邏輯更新。
2.  Process List（Live Process）欄位定義與 WMI 應用見 **使用說明.md**、**開發者說明.md**。
3.  `docs\RegistrySearch說明.md`: 說明 Registry Provider 的運作原理與 LastWriteTime 實作；已補充過渡方案與 Offline Hive 優先說明。
4.  `docs\Registry Artifact Provider – Specificati.md`: Registry 鑑識規範；已補充 Offline Hive 實作與 key 清單。
5.  `docs\EventLogView說明.md`: Event Log View 使用與驗證說明。
6.  `docs\PrefetchView說明.md`: Prefetch View 使用與驗證說明。
7.  **2026-02-02 同步**：`README.md`、`docs\README.md`、`docs\LIMITATIONS.md`、`docs\ARCHITECTURE.md`、`docs\FORENSIC_ASSUMPTIONS.md`、`docs\UI驗證說明.md`（憑證 Subject/SAN/OU/L/S/C/E、簽章 /du、著作權 2026、全文件同步）。
8.  **2026-02-02 補充**：Detach/Restore 與分離視窗圖示統一、啟動預設 System Info；上述文件與 `docs\待修復問題記錄.md` 已一併更新。

---

## 🛠️ 下階段待辦事項 (TODO)

## 🔺 目前真正剩餘工作

### Stable release 收尾（2026-03-20 已完成本輪文件化結案）
- [x] 執行 stable gate，產生 `Release_Verification\stable-release-gate_20260320_054338.json`。
- [x] 完成 `stable-release-verification-record_20260320_054338.md`、`stable-release-checklist_20260320_054338.md` 與 `stable-release-signoff_20260320_054338.md`。
- [x] 本輪 candidate 已完成 sign-off；M1-M7 以 `Blocked` + waiver 方式留痕，M8-M9 以回歸證據記為 `Pass`。

### 原待辦狀態複核（已完成）
- [x] **VSS 快照流程完善**：權限/服務預檢、WMI 錯誤碼對照、失敗回退、多磁碟區一致性警告、HasSnapshotForPath、Dispose/ResolvePath 加固。
- [x] **Offline Hive 離線一致性流程強化**：事件欄位 OfflineHiveSource (VSS/Live)、ConsistencyScope (SingleSnapshot/SingleLive/Mixed)、SnapshotTimeUtc；Mixed 時發出警告；VssSnapshotContext.CreationTimeUtc。
- [x] InMemoryActivityIndex 記憶體控管（可配置 MaxEvents、Eviction、TrimAllQueues）。
- [x] ETW 高頻節流監控（丟棄事件可見化與調參）。
- [x] **MFT UI 整合**：`Load MFT file...` / `Load from Volumes...`、每來源獨立 tab、分頁/篩選/狀態/匯出、移除 Refresh。
- [x] **Docking UI / Detach-restore 基線**：動態分頁右鍵「Detach to Window」/「Restore to Main Window」；工具列 Detach/Restore 按鈕維持。
- [x] **遠端採集模式**：`WinDFIR.Agent` 主控台專案、排程與回傳範例腳本已存在，但不納入目前 UI / Core local stable gate。
- [x] **Raw Disk 讀取**：RawDiskReader 與 OfflineHiveRegistryProvider.AddRawHive 已實作。
- [x] **Live TCP Sent/Recv**：ETW Kernel-Network 計數已接入 Live TCP View。
- [x] **Drill-down / 時區處理 / Recent Files 補強**：相關功能與測試已完成並同步至使用說明。

### 未來更新項目
- [ ] `HostWitness.Agent` 穩定化與遠端部署驗證（不納入目前 UI / Core local stable release gate）。
- [ ] 若未來調整 `manifest` / snapshot / SQLite schema，需先做高風險 review、相容性說明與文件同步。

---
**編譯狀態：** ✅ Build Success；2026-03-20 本機最新 stable gate 與對應測試為 `95 / 95` 通過（較早的 82 / 82 為同日較前一輪歷史結果）。  
**發布狀態：** 本輪 UI / Core local stable candidate 已完成 gate、verification record、checklist 與 sign-off。若後續候選版需要更高證據等級，再補跑 M1-M7 目標環境 matrix。  
**文件更新：** 2026-03-20（補記短期技術債收斂；保留 82 / 82 歷史測試結果並補註最新 stable gate 為 95 / 95；將 Live TCP placeholder 敘述明確標為歷史狀態；並將過期的「尚未完成」敘述改為目前 stable 收尾狀態）；2026-03-18（MFT UI 改為 per-source tabs、`Load from Volumes...` 多選與移除 Refresh；文件同步）；2026-03-13（ProcessView 說明改為引用使用說明/開發者說明）；2026-03-05（待修補：SnapshotExporter 單次查詢、StubProvider 僅 DEBUG）  
**目錄整理：** 2026-02-02 已移除根目錄暫存錯誤日誌（build_err*.txt、build_error*.txt、providers_error*.txt）與 **old_docs/**（24 個歷史說明，已整併至本文件與功能摘要）；新增 `docs\專案進度與目錄說明.md`、`.gitignore` 加入上述錯誤日誌規則。
