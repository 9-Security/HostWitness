# 還有什麼要做的事項

本文件彙整目前 stable 狀態、未來可選項目與歷史待辦狀態，來源：TECH_DEBT.md、待修復問題記錄.md、README.md。UI / Core 本機 stable workflow 的主要功能與本輪 release 文件已完成；目前真正剩餘的工作以中長期技術債與未來候選版若需提高證據等級時補跑目標環境 matrix 為主。

---

## 一、穩定版收尾與後續追蹤

| 項目 | 目前狀態 | 下一步 |
|------|----------|--------|
| **手動驗證矩陣** | `docs/StableReleaseVerificationMatrix.md`、runbook 與自動 gate 已就緒；本輪已產生 `Release_Verification\stable-release-gate_20260320_054338.json`，並於 verification record 中將 M1-M7 如實記為 `Blocked`、M8-M9 記為 `Pass`。 | 若後續候選版需要更高證據等級，再到目標環境補跑 M1-M7，補齊 Pass / Fail / Blocked 與證據。 |
| **Checklist / Verification Record / Sign-off** | 本輪已完成 `stable-release-checklist_20260320_054338.md`、`stable-release-verification-record_20260320_054338.md` 與 `stable-release-signoff_20260320_054338.md`。 | 後續候選版若重開驗證，沿用同格式更新即可；本輪無額外待辦。 |
| **模式 / 告警 / Preflight / Collection Summary** | UI / Core 本機 stable workflow 已落盤；`manifest.json` 已含 `modeProfile`、`preflight`、`collectionSummary`，高風險功能也已有 non-forensic / high-risk 標示。 | 後續若變更 profile、告警或匯出欄位，需同步 README、LIMITATIONS、穩定版邊界與 release 文件。 |
| **輸出格式版本與相容政策** | 目前 stable baseline 已文件化：Snapshot `snapshotFormat = 1.0`、SQLite `schema_version = 2`，輸出變更規則見 `穩定版邊界定義.md`。 | 未來若調整 `manifest` / snapshot / SQLite schema，視為高風險變更，需說明 migration / compatibility。 |
| **完整性與操作鏈結** | Snapshot bundle 已含 `hashes.txt`，stable gate 亦保留 JSON 驗證報告。 | 若未來擴充 append-only operator log 或更完整的 chain-of-custody 摘要，屬後續 hardening 題目，不是本輪 stable blocker。 |
| **現場採集 Profile 收斂 / 高風險功能隔離** | `Forensic Strict`、`Triage Fast`、SOP 與 UI 動作分級文件已建立，Create Dump / Live Registry 已明確標示。 | 後續若新增高風險功能，需延續相同分類與文件同步規則。 |
| **Agent 穩定化延後** | `HostWitness.Agent` 保留於 repo，但不納入目前 UI / Core local stable release gate。 | 延後至未來更新項目；本輪不擴 Agent scope。 |

---

## 二、原列為可選、現已實作

| 項目 | 說明 | 優先級 |
|------|------|--------|
| **Docking 基線** | 版面儲存/還原已實作（File → Save layout now / Restore default layout；關閉時自動儲存），且 detached 視窗位置/大小可隨 layout 一併還原。**完整 Docking UI**（可拖曳/可停靠框架，如 AvalonDock）仍屬後續 UI 重構題目。 | 已實作（Detach + 版面回復） |
| **Index 持久化基線** | SQLite 匯出/開啟已實作（File → Export to SQLite / Open from SQLite）；離線查詢 API：LoadEventsByTimeRange(dbPath, start, end, category)、LoadEventsByCategory(dbPath, category)。Session 還原（last_session）已實作，且含 `sessionSchemaVersion`。**完整持久化**（canonical store / migration）仍待 `TECH_DEBT.md` §3 決策框架定案。 | 已實作（基線） |
| **遠端採集排程/回傳自動化** | Agent 已有；`scripts/ScheduledCollect.ps1`、`scripts/RunCollectAndCopy.ps1` 提供排程與自動回傳範例；見 `遠端採集Agent說明.md`。 | 已實作 |
| **Live TCP Sent/Recv 計數** | 已由 ETW 接上 NetworkStatsAggregator，Live TCP View 顯示 Sent/Recv 位元組與封包數。 | 已實作 |
| **Raw Disk 導入** | Offline Hive：Settings → Raw Disk (Offline Hive) 可設定磁碟/offset/size，重啟後套用。MFT：改為 `Load from Volumes...`，可多選磁區並以每來源獨立 tab 顯示；依序嘗試 raw volume、Backup privilege、VSS，且無 Refresh 重跑。 | 已實作 |
| **Registry 僅 Offline Hive** | Settings 鑑識模式說明已強化；勾選「僅離線登錄檔」時僅使用 Offline Hive。Live 已標示為 non-forensic。 | 已實作 |

---

## 三、Collector 可選強化

| 項目 | 目前狀態 | 可選強化 |
|------|----------|----------|
| **Recent Files / LNK** | 已整合 RecentDocs、Recent LNK、JumpList、DestList；RecentLnkProvider 僅掃描 Recent 路徑並加上最小 raw path fallback。 | Raw 掃描進階 carving 可再擴充。 |
| **Prefetch** | 支援 version 10/13/15（與 17 同 offset）、17/23/26/30。 | 擴充版本已實作。 |
| **Offline Hive** | VSS、離線一致性已強化；Raw Disk 已導入（Settings + AddRawHive）；MFT 磁碟載入則改為 raw volume、Backup privilege、VSS ordered fallback，並以每來源獨立 tab 呈現。 | 已實作。 |

---

## 四、已知風險（已緩解或文件化）

- **檔案鎖定**：VSS 支援；失敗時自動回退即時路徑。
- **Rootkit / API Hooking**：RegistrySearchProvider / LiveProcessProvider 依賴 Win32 API；鑑識優先 Offline Hive。見 LIMITATIONS §2。
- **記憶體 OOM**：Index.MaxEvents 可配置、Eviction 與 Trim；Settings 可調。
- **ETW 節流**：高頻事件可能丟棄；狀態列可見化。

---

## 五、建議修復／測試（已實作）

- **單元測試**：InMemoryActivityIndex 關鍵屬性比對、ProcessKeyCache 同 TTL 內第二呼叫已涵蓋；全部通過。
- **BrowserHistory**：深度上限 8、CancellationToken 已實作；新增 BrowserHistoryProviderTests（Start/Stop 取消路徑）。
- **SnapshotExporterTests**：manifest machineSid 格式驗證與必要欄位（host、machineSid、collectionTime 等）已涵蓋；新增 ExportAsync_Manifest_HasRequiredHostFields。
- **ParserRobustness**：MftParser 空串流、ActivityEventNormalizer 空/空白 Action 已涵蓋；新增 NormalizeAction(null/空/空白) 回傳 Query。

---

## 六、文件與發布

- 改完 code 後執行 **`.\publish.cmd`** 產生 `Release\HostWitness.exe`。
- 目前 stable 收尾文件以 `StableReleaseVerificationMatrix.md`、`StableReleaseChecklist.md`、`StableReleaseVerificationRecordTemplate.md`、`StableReleaseManualVerificationRunbook.md` 為主；後續功能異動時請一併同步本文件、README、LIMITATIONS 與穩定版邊界文件。

---

**文件更新**：2026-03-20（同步本輪 stable gate / verification record / checklist / sign-off 已完成；將 stable 現況改為「本輪已結案，未來若需更高證據等級再補跑 blocked matrix rows」；並維持 Docking / Index 文案為「基線已實作，完整形態仍屬中長期題目」）；2026-03-19（補充 stable update 先以 UI / Core 為主，Agent 穩定化延後至未來更新項目）；2026-03-19（新增穩定版成熟度 TODO：模式與告警落盤、preflight / collection summary、驗證矩陣、輸出格式版本、完整性與 profile 收斂）；2026-03-19（專案狀態同步）；2026-03-18（MFT per-source tabs、`Load from Volumes...` 多選與移除 Refresh）；2026-03-13（專案文件整理）；2026-03-05（可選功能全量實作、建議修復／測試、Collector 與 P0-P2、Code review 修正）


